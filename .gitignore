# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfvars
*.tfvars.json
!*.tfvars.example

# OS / editor cruft
.DS_Store

# Claude Code - personal/local overrides, not shared with the team
.claude/settings.local.json
